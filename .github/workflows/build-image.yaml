name: Build Docker Image
on:
  push:
    branches: [main]
  release:
    types: [published]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: kengo-k/smart-docker-build@main
        with:
          token: ${{ secrets.GITHUB_TOKEN }}